import { LazyLoadProvidersDirective } from './lazy-load-providers.directive';

describe('LazyLoadProvidersDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyLoadProvidersDirective();
    expect(directive).toBeTruthy();
  });
});
